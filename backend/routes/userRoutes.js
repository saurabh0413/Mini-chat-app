const express = require("express");

const router = express.Router(); //creating instance

//this is only for after /api/user endpoint
// router.route("/").post(registerUser);
// router.route("/login", authUser);

module.exports = router